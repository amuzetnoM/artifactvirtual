{
  "name": "celestial-chaos",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && git add dist -f && git commit -m 'Deploy' && git subtree push --prefix dist origin gh-pages",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.34.1",
    "astro": "^5.6.1",
    "sharp": "^0.32.5"
  }
}
